spring:

  flyway:
    enabled: true
    locations: classpath:db/migration

  datasource:
    url: jdbc:postgresql://localhost:5432/gymapp_db
    username: gymapp_user
    password: changeme_local
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate        # won't auto-create tables — we write SQL manually
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

jwt:
  secret: replace-this-with-a-long-random-string-before-production
  expiration-ms: 86400000       # 24 hours

logging:
  level:
    org.springframework.security: DEBUG

fatsecret:
  client-id: 1db59e44b917453e811e445d2fa0d9dc
  client-secret: 7fca6f1bef0c430094d0c09cd45972f9
  token-url: https://oauth.fatsecret.com/connect/token
  api-url: https://platform.fatsecret.com/rest/server.api
  
exercisedb:
  api-url: https://oss.exercisedb.dev/api/v1